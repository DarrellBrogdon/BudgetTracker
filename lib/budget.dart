class Budgets {
  Budgets();

  // factory Budgets.fromJson(Map<String, dynamic> parsedJson) {
  //   return Budgets(parsedJson);
  // }

  // double getBudgetForKey(String key) {
  //   // return budgets?.firstWhere((element) => element.key == key).value;
  //   var foo = budgets?.containsKey(key);

  //   return 9.90;
  // }
}
